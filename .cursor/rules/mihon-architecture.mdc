---
description: Mihon 项目架构规则 - 模块结构、依赖方向、分层模式
alwaysApply: true
---

# Mihon 架构规则

## 模块依赖方向（铁律）

```
app/ → 所有模块
presentation-core/ → domain/, core/, i18n/
data/ → domain/, core/, source-api/
domain/ → core/
source-local/ → source-api/, core/, domain/
```

- `domain/` 定义 Repository 接口，`data/` 提供实现
- 模块间不可循环依赖

## 分层架构

```
UI (Compose) → ScreenModel (Voyager) → Interactor (domain/) → Repository 接口 (domain/) ← 实现 (data/) → SQLDelight
```

- ScreenModel 继承 `StateScreenModel<State>`，State 标注 `@Immutable`
- State 通过 `mutableState.update { it.copy(...) }` 更新
- 业务逻辑不直接写在 Compose 中
- 使用 presentation-core 的 `Scaffold`（不是 material3 的）

## KMP expect/actual

`source-local` 模块使用 KMP：`commonMain` 定义 `expect class`，`androidMain` 提供 `actual class`。新增 expect 声明时**必须同时提供 actual 实现**。

## DI

- 使用 Injekt（Kodein），构造函数注入 `= Injekt.get()`
- Interactor 注册在 `DomainModule.kt`，基础设施注册在 `AppModule.kt`

## 编译验证

```bash
./gradlew :app:compileDebugKotlin    # 编译验证（最常用）
```

注意：不存在 `compileStandardDebugKotlin` 任务。
